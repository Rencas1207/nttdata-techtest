// import clientAxios from './axios.js'

// const tokenAuth = (token) => {
//    token
//       ? clientAxios.defaults.headers.common['AuthToken'] = `Bearer ${token}`
//       : delete clientAxios.defaults.headers.common['AuthToken'];

// }

// export default tokenAuth;